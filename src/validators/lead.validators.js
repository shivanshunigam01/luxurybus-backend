import { z } from 'zod';
export const createLeadSchema = z.object({ body: z.object({ guestName: z.string().optional(), guestEmail: z.string().email().optional().or(z.literal('')), guestPhone: z.string().optional(), pickup: z.string().min(2), drop: z.string().min(2), journeyDate: z.string().min(2), journeyTime: z.string().min(2), returnDate: z.string().optional(), passengers: z.coerce.number().positive(), busType: z.string().optional(), acPreference: z.string().optional(), purpose: z.string().optional(), notes: z.string().optional() }), params: z.object({}).optional(), query: z.object({}).optional() });
